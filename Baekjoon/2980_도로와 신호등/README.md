# [2980] 도로와 신호등

### **난이도**
실버 4
## **📝문제**
상근이는 트럭을 가지고 긴 일직선 도로를 운전하고 있다. 도로에는 신호등이 설치되어 있다. 상근이는 각 신호등에 대해서 빨간 불이 지속되는 시간과 초록 불이 지속되는 시간을 미리 구해왔다. (빨강색과 초록색 불빛은 무한히 반복된다)

상근이의 트럭이 도로에 진입했을 때, 모든 신호등의 색상은 빨간색이고, 사이클이 막 시작한 상태이다. 상근이는 1초에 1미터를 움직인다. 신호등의 색상이 빨간색인 경우에는 그 자리에서 멈추고 초록색으로 바뀔때 까지 기다린다.

상근이가 도로의 끝까지 이동하는데 걸리는 시간을 구하는 프로그램을 작성하시오. 도로의 시작은 0미터이고, 끝은 L미터인 지점이다.
### **입력**
첫째 줄에 신호등의 개수 N과 도로의 길이 L이 주어진다. (1 ≤ N ≤ 100, 1 ≤ L ≤ 1000)

다음 N개 줄에는 각 신호등의 정보 D, R, G가 주어진다. (1 ≤ D < L, 1 ≤ R ≤ 100, 1 ≤ G ≤ 100) D는 신호등의 위치이며, R과 G는 빨간색, 초록색이 지속되는 시간이다.

신호등은 D가 증가하는 순서로 주어지며, 같은 위치에 있는 신호등이 두 개 이상 있는 경우는 없다.
### **출력**
첫째 줄에 상근이가 도로의 끝까지 이동하는데 걸리는 시간을 출력한다.
### **예제입출력**

**예제 입력1**

```
2 10
3 5 5
5 2 2
```

**예제 출력1**

```
12
```

**예제 입력2**

```
4 30
7 13 5
14 4 4
15 3 10
25 1 1
```

**예제 출력2**

```
36
```

### **출처**

## **🧐CODE REVIEW**

### **🧾나의 풀이**

```python
N, L = map(int, input().split())
traffics = [list(map(int, input().split())) for _ in range(N)]

time, pos = 0, 0

for D, R, G in traffics:
    time += (D - pos)
    pos = D

    cycle = R + G
    now = time % cycle

    if now < R:
        time += (R - now)
    
time += (L - pos)
print(time)
```

결과	| 메모리(KB) |	시간(ms) |	언어 |	코드 길이(B)
:----:|:-----:|:-----:|:-----:|:--------:
정답|32412|36|Python3|293
#### **📝해설**

**알고리즘**
```
1. 구현
```

### **다른 풀이**

```python
N,L=map(int,input().split())
li=[]
res=0
now=0
for i in range(N):
    D,R,G=map(int,input().split())
    res+=(D-now)
    now=D
    if res%(R+G)<=R:
        res+=(R-(res%(R+G)))
res+=(L-now)
print(res)
```

아이디 | 메모리(KB) |	시간(ms) |	언어 |	코드 길이(B) 
:-----:|:-----:|:-----:|:----:|:--------:
weon1009|31120|32|Python3|201
#### **📝해설**

```python
N, L = map(int, input().split())
traffics = [list(map(int, input().split())) for _ in range(N)]

time, pos = 0, 0

# 신호등을 확인하면서
for D, R, G in traffics:

    # 신호등의 위치까지 이동시간
    time += (D - pos)

    # 현재 위치
    pos = D

    # 이번 신호등의 사이클
    cycle = R + G

    # 현재 사이클이 돌기까지 남은 시간
    now = time % cycle

    # 만약 빨간불이라면 빨간불을 기다리는 만큼 시간 추가
    if now < R:
        time += (R - now)

# 마지막 신호등 통과 후 끝까지 이동
time += (L - pos)
print(time)
```