# [34053] 도미노 게임

### **난이도**
실버 3
## **📝문제**
$N\times M$ 크기의 격자가 주어진다. 
$i$행 
$j$열의 칸에는 음이 아닌 정수 
$a_{ij}$가 적혀 있다. (
$1\le i\le N$; 
$1\le j\le M$)

민지는 각 차례마다 아래와 같은 행동을 한다.

1. 격자에서 상하좌우로 인접한 두 칸을 선택한다. 이때 두 칸에 적힌 수 중 적어도 하나는 양의 정수이어야 한다.
2. 선택한 칸에 적힌 수에서 
$1$씩 뺀다. 만약 어떤 칸에 적힌 수가 
$0$이라면 그대로 둔다.

모든 칸에 적힌 수가 
$0$이 될 때 게임이 종료된다. 민지는 이 게임을 최대한 오래 하려고 한다. 민지가 최선을 다해 게임을 오래 진행했을 때, 진행할 수 있는 최대 차례의 수를 구해보자.
### **입력**
첫째 줄에 양의 정수 
$N$, 
$M$이 공백으로 구분되어 주어진다. (
$2 \le N, M \le 1\,000$)

이후 
$N$개의 줄에 걸쳐 격자에 적힌 수가 주어진다. 그 중 
$i$번째 줄에는 
$a_{i1}, a_{i2}, \cdots, a_{iM}$이 공백으로 구분되어 주어진다. (
$0 \le a_{ij} \le 10^9$)
### **출력**
진행할 수 있는 최대 차례의 수를 출력한다.
### **예제입출력**

**예제 입력1**

```
2 2
0 1
0 0
```

**예제 출력1**

```
1
```

**예제 입력2**

```
2 2
1 0
1 1
```

**예제 출력2**

```
3
```

### **출처**

## **🧐CODE REVIEW**

### **🧾나의 풀이**

```python
N, M = map(int, input().split())
table = [list(map(int, input().split())) for _ in range(N)]

min_point = int(21e8)
start_x, start_y = 0, 0
for i in range(N):
    for j in range(M):
        if min_point > table[i][j]:
            start_x, start_y = i, j
            min_point = table[i][j]

if min_point != 0:
    table[start_x][start_y] = 0

print(sum(sum(row) for row in table))
```

결과	| 메모리(KB) |	시간(ms) |	언어 |	코드 길이(B)
:----:|:-----:|:-----:|:-----:|:--------:
정답|72704|400|Python3|380
#### **📝해설**

**알고리즘**
```
1. 그리디 알고리즘
```

### **다른 풀이**

```python
from sys import stdin

n,m = map(int, stdin.readline().split())


sum_N = 0  # 수 총합
min_N = -1  # 최소값
for i in range(n):
    li = list(map(int, stdin.readline().split()))
    sum_N += sum(li)
    min_li = min(li)
    if min_N == -1 or min_N > min_li:
        min_N = min_li

print(sum_N - min_N)
```

아이디 | 메모리(KB) |	시간(ms) |	언어 |	코드 길이(B) 
:-----:|:-----:|:-----:|:----:|:--------:
kanhell|32412|220|Python3|307
#### **📝해설**

```python
N, M = map(int, input().split())
table = [list(map(int, input().split())) for _ in range(N)]

# 입력받은 숫자 중 가장 값이 작은 곳을 찾음
min_point = int(21e8)
start_x, start_y = 0, 0
for i in range(N):
    for j in range(M):
        if min_point > table[i][j]:
            start_x, start_y = i, j
            min_point = table[i][j]

# 만약 0인 곳이 없다면, 가장 값이 작은곳부터 시작해서 0으로 만듦
if min_point != 0:
    table[start_x][start_y] = 0

# 나머지의 총 합이 최대값
print(sum(sum(row) for row in table))
```